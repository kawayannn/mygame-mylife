= render 'shared/header'
%main
  .user-data
    = render partial: 'shared/user', locals: { user: @user }, layout: 
    - if @user.id == current_user.id
      プロフィールの変更
    - else
      = render partial: 'shared/follow', locals: { user: @user }
        